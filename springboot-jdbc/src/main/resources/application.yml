######全局配置文件#####

##服务器配置
server:
  context-path: /jdbc
  port: 80

## 日志配置
logging:
  pattern:
    level: DEBUG 
    
## 数据库配置
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql:///study
    username: root
    password: root     

